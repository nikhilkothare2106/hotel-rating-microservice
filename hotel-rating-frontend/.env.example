# Base URL the app calls for /users, /hotels, /ratings.
# This must be a URL reachable from the USER'S BROWSER, not a Docker-internal
# hostname — so use the host machine's address / exposed port, not a service
# name like "gateway:8084".
VITE_API_BASE_URL=http://localhost:8084
