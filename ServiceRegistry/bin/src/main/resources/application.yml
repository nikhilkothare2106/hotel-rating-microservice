spring:
  application:
    name: ServiceRegistry
  output:
    ansi:
      enabled: ALWAYS

server:
  port: ${SERVER_PORT:8761}

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
